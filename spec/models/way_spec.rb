require 'spec_helper'

describe OSM::Way do

  # subject { OSM::Way.first }
	
  # describe "node_ids" do
  #   it "should parse integers in array nodes" do
  #     subject.nodes = "{1,2,3}"
  #     subject.node_ids.should == [ 1, 2, 3 ]
  #   end
  # end 

  # describe "find_all_by_nodes" do
  #   it "should ways with one of the specified nodes" do
  #     OSM::Way.find_all_by_nodes(subject.node_ids).should include(subject)
  #   end
  # end

end
